{
  "scripts": {
    "build": "npm install && PUPPETEER_CACHE_DIR=/opt/render/project/.render/chrome npx puppeteer browsers install chrome",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^21.10.0"
  }
}